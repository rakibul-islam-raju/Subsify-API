const saltRounds = 10;
const jwtSecret = "secret321//";
const JWT_EXPIRES_IN = "1d";

const totalItems = 0;
const limit = 10;
const offset = 0;
const sortType = "desc";
const sortBy = "updatedAt";
const search = "";

export default {
	saltRounds,
	jwtSecret,
	JWT_EXPIRES_IN,
	totalItems,
	limit,
	offset,
	sortType,
	sortBy,
	search,
};
